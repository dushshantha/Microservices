populartimes_api:
  restart: always
  build: ./PopularTimesService
  expose:
    - "5001"
  ports:
    - "5001:5001"
  volumes:
    - /usr/src/app/static
  env_file: .env
  command: /usr/local/bin/gunicorn -w 4 --bind :5001 main:app
